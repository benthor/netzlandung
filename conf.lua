function love.conf(t)
  t.window.title = "Netzlandung. Arrow keys to control ship, ESC to quit"
  t.version = "0.10.2"
  t.identity = "save"
  t.window.resizable = true
  --t.window.borderless = true
  --t.window.fullscreen = true
end

